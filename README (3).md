# AETHER desktop target

This directory contains the target boundary for desktop. The language/runtime remains host-agnostic; platform SDKs, linkers, drivers, and signing toolchains are adapters at this boundary.
