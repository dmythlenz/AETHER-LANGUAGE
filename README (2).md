# AETHER android target

This directory contains the target boundary for android. The language/runtime remains host-agnostic; platform SDKs, linkers, drivers, and signing toolchains are adapters at this boundary.
