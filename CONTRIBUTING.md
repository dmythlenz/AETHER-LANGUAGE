# Contributing

Keep domain logic in AETHER. New host dependencies must be declared as adapter boundaries. Every feature needs a source-level conformance test and documentation. Do not mark a target `native` unless an executing backend exists and passes the target suite.
