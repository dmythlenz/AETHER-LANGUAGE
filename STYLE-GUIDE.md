# AETHER Style Guide

Use `snake_case` for functions and variables, PascalCase for type-like concepts, explicit `mut` for mutation, double quotes for strings, and two-space indentation. Keep host/device effects behind capability-aware functions. Prefer small deterministic functions and Result-shaped error boundaries.
